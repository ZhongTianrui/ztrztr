#include <bits/stdc++.h>

using namespace std;
int input() {
    char x, y, z;
    char qkh;
    char nxt;
    if (nxt >= '0' and nxt <= '9') 
}
int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int T; cin >> T;
    while (T --) {
    }
    return 0;
}