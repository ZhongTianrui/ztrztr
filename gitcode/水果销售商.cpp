#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int T, V; cin >> T >> V;
    while (T --) {
        int a, b, m;
        cin >> a >> b >> m;
        
    }
    return 0;
}